-- lunar x  v5  |  fisch  |
local Players    = game:GetService("Players")
local RunService = game:GetService("RunService")
local RepStore   = game:GetService("ReplicatedStorage")
local TeleServ   = game:GetService("TeleportService")
local TS         = game:GetService("TweenService")
local VIM        = game:GetService("VirtualInputManager")
local UIS        = game:GetService("UserInputService")
local HTTP       = game:GetService("HttpService")
local SGui       = game:GetService("StarterGui")

local lp   = Players.LocalPlayer
local pgui = lp:WaitForChild("PlayerGui")

if _G.__lx then _G.__lx = false task.wait(0.1) end
_G.__lx = true

-- ── remotes ───────────────────────────────────────────────────────────────────
local RC, castRemote
pcall(function() RC = require(RepStore.client.legacyControllers.ReelController) end)
pcall(function()
    castRemote = require(RepStore:WaitForChild("packages"):WaitForChild("Net")):RemoteFunction("FishingRod/Cast")
end)

local function rgb(r,g,b) return Color3.fromRGB(r,g,b) end

-- ── themes ───────────────────────────────────────────────────────────────────
local THEMES = {
    Default  = {bg=rgb(13,13,18),  panel=rgb(9,9,13),   el=rgb(20,20,30),  elh=rgb(27,27,40),  bord=rgb(36,34,54), acc=rgb(88,125,255), atxt=rgb(255,255,255),txt=rgb(230,228,250),sub=rgb(108,106,138),dim=rgb(48,46,70),  green=rgb(46,196,96), red=rgb(215,55,55), yell=rgb(240,185,50),cyan=rgb(30,200,245), purp=rgb(160,90,255)},
    Midnight = {bg=rgb(8,8,12),    panel=rgb(5,5,9),    el=rgb(14,14,22),  elh=rgb(20,20,30),  bord=rgb(28,26,44), acc=rgb(145,90,255), atxt=rgb(255,255,255),txt=rgb(228,225,255),sub=rgb(98,96,138),  dim=rgb(38,36,62), green=rgb(46,196,96), red=rgb(215,55,55), yell=rgb(240,185,50),cyan=rgb(30,200,245), purp=rgb(190,100,255)},
    Synthwave= {bg=rgb(16,6,26),   panel=rgb(11,4,18),  el=rgb(28,10,44),  elh=rgb(38,14,58),  bord=rgb(62,18,88), acc=rgb(255,45,200), atxt=rgb(255,255,255),txt=rgb(255,228,255),sub=rgb(165,78,190), dim=rgb(60,18,82), green=rgb(50,220,130),red=rgb(255,60,100),yell=rgb(255,200,50),cyan=rgb(0,245,255),  purp=rgb(200,60,255)},
    Carbon   = {bg=rgb(11,11,11),  panel=rgb(7,7,7),    el=rgb(19,19,19),  elh=rgb(26,26,26),  bord=rgb(34,34,34), acc=rgb(210,210,210),atxt=rgb(15,15,15),  txt=rgb(235,235,235),sub=rgb(115,115,115),dim=rgb(42,42,42), green=rgb(80,200,80), red=rgb(200,55,55), yell=rgb(220,175,50),cyan=rgb(80,195,220), purp=rgb(160,100,220)},
    Ocean    = {bg=rgb(6,14,24),   panel=rgb(4,10,18),  el=rgb(10,22,38),  elh=rgb(14,30,50),  bord=rgb(18,40,64), acc=rgb(30,195,225), atxt=rgb(255,255,255),txt=rgb(218,240,255),sub=rgb(88,138,178), dim=rgb(18,40,68), green=rgb(46,210,140),red=rgb(215,65,65), yell=rgb(240,195,55),cyan=rgb(30,220,255), purp=rgb(130,90,255)},
}
local T = {}
for k,v in THEMES.Default do T[k]=v end

local colorRegs = {}
local function reg(inst,key,prop)
    table.insert(colorRegs,{inst=inst,key=key,prop=prop or "BackgroundColor3"})
    return inst
end
local function applyTheme(name)
    local th=THEMES[name]; if not th then return end
    for k,v in th do T[k]=v end
    for _,r in colorRegs do
        if T[r.key] then TS:Create(r.inst,TweenInfo.new(0.3,Enum.EasingStyle.Quart),{[r.prop]=T[r.key]}):Play() end
    end
end

-- ── events list ───────────────────────────────────────────────────────────────
local EVENTS = {
    {name="Great White Shark",        zone="Great White Shark",        hunt=true },
    {name="Whale Shark",              zone="Whale Shark",              hunt=true },
    {name="Great Hammerhead Shark",   zone="Great Hammerhead Shark",   hunt=true },
    {name="Megalodon",                zone="Megalodon Default",                hunt=true },
    {name="Shamrock Megalodon",       zone="Shamrock Megalodon",       hunt=true },
    {name="Scylla",                   zone="Forsaken Veil - Scylla",   hunt=true },
    {name="Leviathan",                zone="Leviathan",                hunt=true },
    {name="Skeletal Leviathan",       zone="Skeletal Leviathan Hunt",  hunt=false},
    {name="Shamrock Leviathan",       zone="Shamrock Leviathan",       hunt=true },
    {name="Colossal Ethereal Dragon", zone="Colossal Ethereal Dragon", hunt=true },
    {name="Colossal Blue Dragon",     zone="Colossal Blue Dragon",     hunt=true },
    {name="Colossal Ancient Dragon",  zone="Colossal Ancient Dragon",  hunt=true },
    {name="Dreadfin",                 zone="Dreadfin",                 hunt=true },
    {name="Mossjaw",                  zone="Mossjaw",                  hunt=true },
    {name="Elder Mossjaw",            zone="Elder Mossjaw",            hunt=true },
    {name="Frostwyrm",                zone="Frostwyrm",                hunt=true },
    {name="Flower Guardian",          zone="Flower Guardian",          hunt=true },
    {name="Omnithal",                 zone="Omnithal",                 hunt=true },
    {name="Plesiosaur",               zone="Plesiosaur",               hunt=true },
    {name="Rotbloom",                 zone="Rotbloom",                 hunt=true },
    {name="Kraken",                   zone="Kraken",                   hunt=true },
    {name="Orca",                     zone="Orca",                     hunt=true },
    {name="Whale",                    zone="Whale",                    hunt=true },
    {name="Isonade",                  zone="Isonade",                  hunt=true },
    {name="Baby Bloop Fish",          zone="Baby Bloop Fish",          hunt=false},
    {name="Bloop Fish",          zone="Bloop Fish",          hunt=true},
}
-- events that need higher Y offset
local HIGH_Y    = {["Omnithal"]=true,["Awakened Omnithal"]=true,["Scylla"]=true}
-- events that need extra forward (negative Z) offset because creature is far back
local FORWARD_Z = {["Orca"]=true, ["Whale"]=true, ["Whale Shark"]=true}

local eventEnabled = {}
for _,e in EVENTS do eventEnabled[e.name]=false end

-- ── file paths  (all in lunarx/ folder) ──────────────────────────────────────
local FOLDER      = "lunarx"
local CFG_FILE    = FOLDER.."/cfg.json"
local SLOTS_FILE  = FOLDER.."/slots.json"
local EVENTS_FILE = FOLDER.."/events.json"
local FARM_FILE   = FOLDER.."/farm.json"
local HOP_FILE    = FOLDER.."/hop.json"

pcall(function() if makefolder and not isfolder(FOLDER) then makefolder(FOLDER) end end)

-- ── config ────────────────────────────────────────────────────────────────────
local slots, slotNames, slotCount = {}, {}, 5

local cfg = {
    AutoCast=false, AutoShake=false, AutoWin=false, PerfectCatch=false,
    InstantBite=false, ProgressSpeed=100,
    WalkSpeed=16, JumpPower=50, WalkOnWater=false, NoClip=false,
    Frozen=false, GodMode=false, AntiVoid=false, AntiAFK=false,
    AutoStarCrater=false, StarCraterServerHop=true,
    FarmSpeed=1.0,    -- multiplier: lower = faster (0.1 to 3.0)
    FarmDelay=0.2,    -- wait between craters in seconds
    StartDelay=2,     -- seconds before farm loop starts on script load
    AutoEvent=false, EventReturnSlot=nil,
    BoatFly=false, BoatFlySpeed=80, BoatNoClip=false, BoatFreeze=false,
    WebhookURL="", WHEnabled=false, WHShowCatch=true, WHShowLevel=true,
    WHShowPlayer=true, WHShowTime=true, WHEventStart=true, WHAutoFarm=true,
    Theme="Default", AutoLoadPreset=false,
}

local function saveCfg()
    pcall(function()
        if not writefile then return end
        local t={}; for k,v in cfg do if type(v)~="nil" then t[k]=v end end
        writefile(CFG_FILE, HTTP:JSONEncode(t))
    end)
    pcall(function()
        if not writefile then return end
        writefile(EVENTS_FILE, HTTP:JSONEncode(eventEnabled))
    end)
end
local function loadCfg()
    pcall(function()
        if not readfile then return end
        local ok,raw=pcall(readfile,CFG_FILE); if not ok then return end
        local d=HTTP:JSONDecode(raw)
        for k,v in d do if cfg[k]~=nil then cfg[k]=v end end
    end)
    pcall(function()
        if not readfile then return end
        local ok,raw=pcall(readfile,EVENTS_FILE); if not ok then return end
        local d=HTTP:JSONDecode(raw)
        for k,v in d do if eventEnabled[k]~=nil then eventEnabled[k]=v end end
    end)
end

local function saveSlots()
    pcall(function()
        if not writefile then return end
        local t={count=slotCount}
        for i=1,slotCount do
            if slots[i] then
                local c=slots[i]
                local _,_,_,r00,r01,r02,r10,r11,r12,r20,r21,r22=c:GetComponents()
                t[tostring(i)]={x=c.X,y=c.Y,z=c.Z,r00=r00,r01=r01,r02=r02,r10=r10,r11=r11,r12=r12,r20=r20,r21=r21,r22=r22,name=slotNames[i]}
            elseif slotNames[i] then t[tostring(i)]={name=slotNames[i]} end
        end
        writefile(SLOTS_FILE, HTTP:JSONEncode(t))
    end)
end
local function loadSlots()
    pcall(function()
        if not readfile then return end
        local ok,raw=pcall(readfile,SLOTS_FILE); if not ok then return end
        local d=HTTP:JSONDecode(raw)
        if d.count then slotCount=math.max(5,d.count) end
        for i=1,slotCount do
            local s=d[tostring(i)]
            if s then
                if s.x then
                    slots[i]=s.r00 and CFrame.new(s.x,s.y,s.z,s.r00,s.r01,s.r02,s.r10,s.r11,s.r12,s.r20,s.r21,s.r22) or CFrame.new(s.x,s.y,s.z)
                end
                slotNames[i]=s.name
            end
        end
    end)
end

local starCraterServerCount = 0
local function saveFarmCount()
    pcall(function() if writefile then writefile(FARM_FILE, HTTP:JSONEncode({count=starCraterServerCount})) end end)
end
local function loadFarmCount()
    pcall(function()
        if not readfile then return end
        local ok,raw=pcall(readfile,FARM_FILE); if not ok then return end
        local d=HTTP:JSONDecode(raw)
        if d.count then starCraterServerCount=d.count end
    end)
end
local function saveHopFlag(pending)
    pcall(function() if writefile then writefile(HOP_FILE, HTTP:JSONEncode({pending=pending,farmOn=cfg.AutoStarCrater})) end end)
end
local function popHopFlag()
    local got, farmWasOn = false, false
    pcall(function()
        if not readfile then return end
        local ok,raw=pcall(readfile,HOP_FILE); if not ok then return end
        local d=HTTP:JSONDecode(raw)
        if d and d.pending then got=true; farmWasOn=d.farmOn; saveHopFlag(false) end
    end)
    return got, farmWasOn
end

loadCfg(); loadSlots(); loadFarmCount()

-- ── debug ─────────────────────────────────────────────────────────────────────
local debugLog={}; local debugLbl=nil
local function dbg(msg)
    local t=os.date("%H:%M:%S")
    table.insert(debugLog,1,"["..t.."] "..tostring(msg))
    if #debugLog>120 then table.remove(debugLog) end
    if debugLbl then debugLbl.Text=table.concat(debugLog,"\n") end
end

-- ── helpers ───────────────────────────────────────────────────────────────────
local function hrp()      return lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") end
local function hum()      return lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") end
local function swimming() local h=hum(); return h and h:GetState()==Enum.HumanoidStateType.Swimming end
local function chatOpen() return UIS:GetFocusedTextBox()~=nil end
local function notify(t,b) pcall(function() SGui:SetCore("SendNotification",{Title=t,Text=b,Duration=4}) end) end
local function getCF(obj)
    if not obj then return nil end
    if obj:IsA("Model") then return obj:GetPivot() end
    if obj:IsA("BasePart") then return obj.CFrame end
    local p=obj:FindFirstChildWhichIsA("BasePart"); return p and p.CFrame
end

-- ── webhook ───────────────────────────────────────────────────────────────────
local catchCount=0; local levelUpTime=tick()
local function httpPost(url,body)
    if request              then return request({Url=url,Method="POST",Headers={["Content-Type"]="application/json"},Body=body})
    elseif http_request     then return http_request({Url=url,Method="POST",Headers={["Content-Type"]="application/json"},Body=body})
    elseif syn and syn.request then return syn.request({Url=url,Method="POST",Headers={["Content-Type"]="application/json"},Body=body})
    else error("no http") end
end
local function fmtTime(s)
    s=math.floor(s)
    if s<60   then return s.."s" end
    if s<3600 then return math.floor(s/60).."m "..s%60 .."s" end
    return math.floor(s/3600).."h "..math.floor(s%3600/60).."m"
end
local function sendWebhook(kind,extra)
    if not cfg.WHEnabled or cfg.WebhookURL=="" then return end
    task.spawn(function()
        local ls=lp:FindFirstChild("leaderstats"); local lv=ls and ls:FindFirstChild("Level")
        local fields={}
        if cfg.WHShowPlayer then table.insert(fields,{name="Player",value=lp.Name,inline=true}) end
        if cfg.WHShowLevel  then table.insert(fields,{name="Level", value=tostring(lv and lv.Value or "?"),inline=true}) end
        local titles={levelup="level up",event="event spotted",test="test ping",autofarm="farm complete"}
        local colors={levelup=0x4163E8,event=0x9055F0,test=0x22C55E,autofarm=0xFFD700}
        if kind=="levelup" then
            if cfg.WHShowCatch then table.insert(fields,{name="Catch #",value=tostring(catchCount),inline=true}) end
            if cfg.WHShowTime  then table.insert(fields,{name="Time",  value=fmtTime(tick()-levelUpTime),inline=true}) end
        elseif kind=="event" then table.insert(fields,{name="Event",value=tostring(extra),inline=true})
        elseif kind=="autofarm" then table.insert(fields,{name="Servers",value=tostring(starCraterServerCount),inline=true}) end
        pcall(httpPost,cfg.WebhookURL,HTTP:JSONEncode({username="lunar x",embeds={{title=titles[kind] or "ping",color=colors[kind] or 0x4163E8,fields=fields,footer={text="lunar x  ·  "..game.PlaceId}}}}))
    end)
end

task.spawn(function()
    local ls=lp:WaitForChild("leaderstats",15); if not ls then return end
    local lv=ls:WaitForChild("Level",15); if not lv then return end
    local lastLv=tonumber(lv.Value) or 0
    local c; c=lv:GetPropertyChangedSignal("Value"):Connect(function()
        if not _G.__lx then c:Disconnect(); return end
        local cur=tonumber(lv.Value) or 0
        if cur>lastLv then lastLv=cur; sendWebhook("levelup"); levelUpTime=tick(); dbg("levelup: "..cur) end
    end)
end)

-- ── god mode (metatable hook) ─────────────────────────────────────────────────
pcall(function()
    local mt=getrawmetatable(game); local oldNI=mt.__newindex
    setreadonly(mt,false)
    mt.__newindex=function(t,k,v)
        if cfg.GodMode then
            local h=hum()
            if h and t==h and k=="Health" and type(v)=="number" and v<h.MaxHealth then return rawset(t,k,h.MaxHealth) end
        end
        return oldNI(t,k,v)
    end
    setreadonly(mt,true)
end)

-- ── walk on water ─────────────────────────────────────────────────────────────
local waterPart,walkConn,waterY
local function enableWater()
    if waterPart then return end
    waterPart=Instance.new("Part"); waterPart.Name="_lxW"
    waterPart.Size=Vector3.new(80,.05,80); waterPart.Anchored=true
    waterPart.CanCollide=true; waterPart.Transparency=1
    waterPart.CastShadow=false; waterPart.CanTouch=false; waterPart.Parent=workspace
    waterY=nil
    walkConn=RunService.Heartbeat:Connect(function()
        local h=hrp(); if not h or not cfg.WalkOnWater then return end
        local st=hum() and hum():GetState()
        if st==Enum.HumanoidStateType.Swimming then waterY=h.Position.Y-2.5
        elseif not waterY then waterY=h.Position.Y-2.9 end
        waterPart.CFrame=CFrame.new(h.Position.X,waterY,h.Position.Z)
    end)
end
local function disableWater()
    if walkConn then walkConn:Disconnect(); walkConn=nil end
    if waterPart then waterPart:Destroy(); waterPart=nil end; waterY=nil
end

-- ── no clip ───────────────────────────────────────────────────────────────────
local noclipConn,origCollide=nil,{}
local function setNoClip(v)
    if noclipConn then noclipConn:Disconnect(); noclipConn=nil end
    if not v then
        for p,w in origCollide do if p and p.Parent then p.CanCollide=w end end
        origCollide={}; return
    end
    origCollide={}
    if lp.Character then for _,p in lp.Character:GetDescendants() do if p:IsA("BasePart") and not p.Anchored then origCollide[p]=p.CanCollide end end end
    noclipConn=RunService.Stepped:Connect(function()
        if not lp.Character or swimming() then return end
        for _,p in lp.Character:GetDescendants() do if p:IsA("BasePart") and not p.Anchored then p.CanCollide=false end end
    end)
end

-- ── freeze (legs + arms only) ─────────────────────────────────────────────────
local frozenCF,freezeConn=nil,nil
local FLIMBS={"LeftFoot","RightFoot","LeftLowerLeg","RightLowerLeg","LeftHand","RightHand","LeftLowerArm","RightLowerArm"}
local function setFrozen(v)
    if freezeConn then freezeConn:Disconnect(); freezeConn=nil end
    if not v then return end
    local h=hrp(); if h then frozenCF=h.CFrame end
    freezeConn=RunService.Heartbeat:Connect(function()
        if not cfg.Frozen then return end
        local h2=hrp()
        if h2 and frozenCF then
            h2.CFrame=CFrame.new(frozenCF.Position)*(h2.CFrame-h2.CFrame.Position)
            for _,n in FLIMBS do
                local p=lp.Character and lp.Character:FindFirstChild(n)
                if p and p:IsA("BasePart") then p.AssemblyLinearVelocity=Vector3.zero; p.AssemblyAngularVelocity=Vector3.zero end
            end
        end
    end)
end

-- ── god mode heartbeat ────────────────────────────────────────────────────────
local godConn
local function setGodMode(v)
    if godConn then godConn:Disconnect(); godConn=nil end
    if not v then return end
    godConn=RunService.Heartbeat:Connect(function() local h=hum(); if h then rawset(h,"Health",h.MaxHealth) end end)
end

-- ── anti void ─────────────────────────────────────────────────────────────────
local lastSafe=nil
RunService.Heartbeat:Connect(function()
    local h=hrp(); if not h then return end
    if h.Position.Y>5 and not swimming() then lastSafe=h.Position end
    if cfg.AntiVoid and h.Position.Y<-60 then
        h.CFrame=CFrame.new(lastSafe and lastSafe+Vector3.new(0,25,0) or Vector3.new(0,50,0)); dbg("antivoid")
    end
end)

-- ── anti afk ─────────────────────────────────────────────────────────────────
task.spawn(function()
    while _G.__lx and task.wait(55) do
        if cfg.AntiAFK then
            pcall(function() VIM:SendKeyEvent(true,Enum.KeyCode.LeftAlt,false,game); task.wait(0.08); VIM:SendKeyEvent(false,Enum.KeyCode.LeftAlt,false,game) end)
        end
    end
end)

-- ── boat ──────────────────────────────────────────────────────────────────────
local lastBoatModel,boatOrigAnchor,boatPreAnchor={},{},{}
local boatFlyConn,boatNcConn,boatFrConn; local boatFrozenCF=nil
local function getBoat()
    local h=hum(); if not h then return nil end
    local seat=h.SeatPart; if not seat then return nil end
    local m=seat:FindFirstAncestorWhichIsA("Model")
    return (m and m~=workspace) and m or nil
end
local function getBoatRoot(b)
    if not b then return nil end; if b.PrimaryPart then return b.PrimaryPart end
    local best,bv=nil,0
    for _,p in b:GetDescendants() do if p:IsA("BasePart") then local v=p.Size.X*p.Size.Y*p.Size.Z; if v>bv then best=p;bv=v end end end
    return best
end
task.spawn(function()
    while task.wait(0.5) do
        local boat=getBoat()
        if boat and boat~=lastBoatModel then lastBoatModel=boat; boatOrigAnchor={}; for _,p in boat:GetDescendants() do if p:IsA("BasePart") then boatOrigAnchor[p]=p.Anchored end end end
    end
end)
local function setBoatFly(v)
    if boatFlyConn then boatFlyConn:Disconnect(); boatFlyConn=nil end; if not v then return end
    boatFlyConn=RunService.Heartbeat:Connect(function(dt)
        if not cfg.BoatFly then return end
        local boat=getBoat(); local root=getBoatRoot(boat); if not root then return end
        if chatOpen() then root.AssemblyLinearVelocity=Vector3.zero; root.AssemblyAngularVelocity=Vector3.zero; return end
        local cam=workspace.CurrentCamera; local cf=root.CFrame; local mv=Vector3.zero
        local look=cam.CFrame.LookVector; local right=cam.CFrame.RightVector
        if UIS:IsKeyDown(Enum.KeyCode.W) then mv=mv+look end
        if UIS:IsKeyDown(Enum.KeyCode.S) then mv=mv-look end
        if UIS:IsKeyDown(Enum.KeyCode.A) then mv=mv-right end
        if UIS:IsKeyDown(Enum.KeyCode.D) then mv=mv+right end
        if UIS:IsKeyDown(Enum.KeyCode.E) then mv=mv+Vector3.new(0,1,0) end
        if UIS:IsKeyDown(Enum.KeyCode.Q) then mv=mv-Vector3.new(0,1,0) end
        if mv.Magnitude>0 then root.CFrame=CFrame.new(cf.Position+mv.Unit*cfg.BoatFlySpeed*dt)*(cf-cf.Position) end
        root.AssemblyLinearVelocity=Vector3.zero; root.AssemblyAngularVelocity=Vector3.zero
        for _,p in boat:GetDescendants() do if p:IsA("BasePart") and p~=root then p.AssemblyLinearVelocity=Vector3.zero; p.AssemblyAngularVelocity=Vector3.zero end end
    end)
end
local function setBoatNoClip(v)
    if boatNcConn then boatNcConn:Disconnect(); boatNcConn=nil end; if not v then return end
    boatNcConn=RunService.Stepped:Connect(function()
        if not cfg.BoatNoClip then return end
        local b=getBoat(); if not b then return end
        for _,p in b:GetDescendants() do if p:IsA("BasePart") and not p.Anchored then p.CanCollide=false end end
    end)
end
local function setBoatFreeze(v)
    if boatFrConn then boatFrConn:Disconnect(); boatFrConn=nil end
    boatFrozenCF=nil; if not v then return end
    local r=getBoatRoot(getBoat()); if r then boatFrozenCF=r.CFrame end
    boatFrConn=RunService.Heartbeat:Connect(function()
        if not cfg.BoatFreeze then return end
        local r2=getBoatRoot(getBoat())
        if r2 and boatFrozenCF then r2.CFrame=boatFrozenCF; r2.AssemblyLinearVelocity=Vector3.zero; r2.AssemblyAngularVelocity=Vector3.zero end
    end)
end

-- ── event CF lookup ───────────────────────────────────────────────────────────
local function getEventCF(ev)
    local zn=ev.hunt and (ev.zone.." Hunt") or ev.zone
    local fz=workspace:FindFirstChild("zones") and workspace.zones:FindFirstChild("fishing")
    if fz then
        local z=fz:FindFirstChild(zn) or fz:FindFirstChild(ev.zone)
        if z then
            if z:IsA("BasePart") then return z.CFrame end
            if z:IsA("Model") then return z:GetPivot() end
            local p=z:FindFirstChildWhichIsA("BasePart"); if p then return p.CFrame end
        end
    end
    local function scan(parent,depth)
        if depth>6 then return nil end
        for _,o in parent:GetChildren() do
            local n=o.Name
            if n==zn or n==ev.zone or n==ev.zone.." Hunt" then
                if o:IsA("BasePart") then return o.CFrame end
                if o:IsA("Model") then return o:GetPivot() end
                local p=o:FindFirstChildWhichIsA("BasePart"); if p then return p.CFrame end
            end
            local r=scan(o,depth+1); if r then return r end
        end
    end
    return scan(workspace,0)
end

-- ── event auto-TP loop ────────────────────────────────────────────────────────
local activeEvent,lastEventPos=nil,nil
local cancelledEvent=nil   -- name of event to skip ONCE

local function cancelCurrentEvent()
    if activeEvent then
        cancelledEvent=activeEvent.name
        cfg.Frozen=false
        if freezeConn then freezeConn:Disconnect(); freezeConn=nil end
        notify("event","skipped "..activeEvent.name.." — will re-enable next time")
        activeEvent=nil; lastEventPos=nil
    end
end

task.spawn(function()
    while task.wait(2) do
        if not cfg.AutoEvent then
            if activeEvent then cfg.Frozen=false; if freezeConn then freezeConn:Disconnect(); freezeConn=nil end; activeEvent=nil; lastEventPos=nil end
            continue
        end
        if activeEvent then
            local cf=getEventCF(activeEvent)
            if cf then
                local np=cf.Position; local first=not lastEventPos
                local moved=not first and (np-lastEventPos).Magnitude>5
                if first or moved then
                    local yOff=HIGH_Y[activeEvent.name] and 50 or 30
                    -- extra forward offset for surface creatures
                    local zOff=FORWARD_Z[activeEvent.name] and -35 or 0
                    local dest=CFrame.new(np.X, np.Y+yOff, np.Z+zOff)
                    local h=hrp()
                    if h then h.CFrame=dest; frozenCF=dest; if not cfg.Frozen then cfg.Frozen=true; setFrozen(true) end end
                    lastEventPos=np
                end
            else
                local h=hrp()
                if cfg.EventReturnSlot and slots[cfg.EventReturnSlot] and h then
                    if freezeConn then freezeConn:Disconnect(); freezeConn=nil end
                    h.CFrame=slots[cfg.EventReturnSlot]; task.wait(0.05)
                end
                cfg.Frozen=false; if freezeConn then freezeConn:Disconnect(); freezeConn=nil end
                notify("event",activeEvent.name.." gone"); activeEvent=nil; lastEventPos=nil
            end
            continue
        end
        for _,ev in EVENTS do
            if not eventEnabled[ev.name] then continue end
            if ev.name==cancelledEvent then continue end  -- skip once
            local cf=getEventCF(ev)
            if cf then
                activeEvent=ev; lastEventPos=nil
                cancelledEvent=nil  -- clear cancel once a NEW event is found
                notify("event",ev.name); sendWebhook("event",ev.name); dbg("event: "..ev.name); break
            end
        end
    end
end)

-- ── shop / interactable ───────────────────────────────────────────────────────
local returnCF=nil; local returnBtnRef=nil
local function findInteractable(name)
    local world=workspace:FindFirstChild("world")
    local ints=world and world:FindFirstChild("interactables")
    if ints then local r=ints:FindFirstChild(name); if r then return r end end
    return workspace:FindFirstChild(name)
end
local function useInteractable(name)
    local h=hrp(); if not h then return end
    local model=findInteractable(name); if not model then notify("shop",name.." not found"); return end
    local cf=getCF(model); if not cf then return end
    returnCF=h.CFrame
    if returnBtnRef then returnBtnRef.Visible=true end
    h.CFrame=cf*CFrame.new(0,4,5); task.wait(0.5)
    VIM:SendKeyEvent(true,Enum.KeyCode.E,false,game); task.wait(0.15)
    VIM:SendKeyEvent(false,Enum.KeyCode.E,false,game)
end

-- ── star crater farm ──────────────────────────────────────────────────────────
local function findStarCraters()
    local found={}
    local function scanParent(p)
        for _,v in p:GetChildren() do
            local n=v.Name:lower()
            if n:find("star crater",1,true) or n:find("starcrater",1,true) then table.insert(found,v) end
            if v:IsA("Model") or v:IsA("Folder") then scanParent(v) end
        end
    end
    local world=workspace:FindFirstChild("world")
    local ints=world and world:FindFirstChild("interactables")
    if ints then scanParent(ints) end
    scanParent(workspace)
    return found
end

local function fireCrater(model)
    local h=hrp(); if not h or not model or not model.Parent then return end
    local cf=getCF(model); if not cf then return end
    h.CFrame=cf*CFrame.new(0,2,2); task.wait(0.15)
    local prompt=nil
    for _,d in model:GetDescendants() do if d:IsA("ProximityPrompt") then prompt=d; break end end
    if prompt and fireproximityprompt then pcall(fireproximityprompt,prompt); task.wait(0.2); return end
    local t0=tick()
    while (tick()-t0)<0.8 and model.Parent do
        VIM:SendKeyEvent(true,Enum.KeyCode.E,false,game); task.wait(0.05)
        VIM:SendKeyEvent(false,Enum.KeyCode.E,false,game); task.wait(0.05)
    end
end

-- ── server hop ────────────────────────────────────────────────────────────────
local function doServerHop()
    pcall(function()
        saveHopFlag(true)
        saveCfg(); saveSlots(); saveFarmCount()

        -- QOT — try every known variant
        local qot = queue_on_teleport or queueonteleport
            or (syn and (syn.queue_on_teleport or syn.queueonteleport))
            or (fluxus and fluxus.queue_on_teleport)
            or (getgenv and getgenv().queue_on_teleport)
        if qot then
            pcall(qot, 'loadstring(game:HttpGet("https://raw.githubusercontent.com/nick2132131/rscripts/refs/heads/main/fisch"))()')
            task.wait(0.5)
            dbg("QOT queued")
        else
            dbg("QOT not available — will not auto-restart")
        end

        -- find a server with room
        local function getPage(cursor)
            local url="https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Desc&limit=100"
            if cursor then url=url.."&cursor="..cursor end
            local ok,raw=pcall(game.HttpGet,game,url)
            return ok and HTTP:JSONDecode(raw) or nil
        end

        local cursor=nil; local servers={}
        for _=1,5 do
            local data=getPage(cursor); if not data or not data.data then break end
            for _,s in pairs(data.data) do
                if s.id~=game.JobId and (s.playing or 0)<17 then table.insert(servers,s.id) end
            end
            cursor=data.nextPageCursor; if not cursor then break end
            task.wait(0.05)
        end

        if #servers>0 then
            local id=servers[math.random(1,#servers)]
            dbg("hop → "..id:sub(1,8))
            TeleServ:TeleportToPlaceInstance(game.PlaceId,id,lp)
        else
            dbg("hop: fallback rejoin")
            saveHopFlag(false)
            TeleServ:Teleport(game.PlaceId,lp)
        end
    end)
end

-- ── farm loop ─────────────────────────────────────────────────────────────────
local starFarmRunning=false
local starFarmStatus=""
local starStatusLbl=nil; local starStatusDot=nil
local farmHudLbl=nil     -- set after GUI

task.spawn(function()
    task.wait(cfg.StartDelay)
    local arrivedFromHop, farmWasOn = popHopFlag()
    if arrivedFromHop then
        starCraterServerCount=starCraterServerCount+1; saveFarmCount()
        sendWebhook("autofarm"); dbg("arrived — total: "..starCraterServerCount)
        if starStatusLbl then starStatusLbl.Text="server #"..starCraterServerCount.." — ready" end
        -- restore auto farm if it was on
        if farmWasOn then cfg.AutoStarCrater=true end
    end

    while _G.__lx and task.wait(0.3) do
        if not cfg.AutoStarCrater then
            if starFarmRunning then
                starFarmRunning=false
                if starStatusLbl then starStatusLbl.Text="idle" end
                if starStatusDot then TS:Create(starStatusDot,TweenInfo.new(0.3),{BackgroundColor3=T.dim}):Play() end
                if farmHudLbl    then farmHudLbl.Text="auto farm: off" end
            end
            continue
        end
        if starFarmRunning then continue end
        starFarmRunning=true

        if farmHudLbl then farmHudLbl.Text="auto farm: scanning..." end
        local craters=findStarCraters()
        dbg("craters: "..#craters)

        if #craters==0 then
            starFarmStatus="no craters — hopping..."
            if starStatusLbl then starStatusLbl.Text=starFarmStatus end
            if starStatusDot then TS:Create(starStatusDot,TweenInfo.new(0.3),{BackgroundColor3=T.yell}):Play() end
            if farmHudLbl    then farmHudLbl.Text="auto farm: hopping..." end
            task.wait(0.1)
            if cfg.AutoStarCrater and cfg.StarCraterServerHop then
                doServerHop(); task.wait(20)
            end
            starFarmRunning=false; continue
        end

        for i,crater in craters do
            if not cfg.AutoStarCrater or not _G.__lx or not crater.Parent then break end
            starFarmStatus="crater "..i.."/"..#craters
            if starStatusLbl then starStatusLbl.Text=starFarmStatus end
            if starStatusDot then TS:Create(starStatusDot,TweenInfo.new(0.3),{BackgroundColor3=T.green}):Play() end
            if farmHudLbl    then farmHudLbl.Text="auto farm: "..i.."/"..#craters end
            fireCrater(crater)
            task.wait(cfg.FarmDelay * cfg.FarmSpeed)
        end

        if not cfg.AutoStarCrater or not _G.__lx then starFarmRunning=false; continue end

        starFarmStatus="done — hopping..."
        if starStatusLbl then starStatusLbl.Text=starFarmStatus end
        if farmHudLbl    then farmHudLbl.Text="auto farm: done, hopping..." end

        if cfg.StarCraterServerHop then
            doServerHop(); task.wait(20); saveHopFlag(false)
        else
            starCraterServerCount=starCraterServerCount+1; saveFarmCount(); sendWebhook("autofarm")
        end
        starFarmRunning=false
    end
end)

-- ══════════════════════════════════════════════════════
--  GUI
-- ══════════════════════════════════════════════════════
if pgui:FindFirstChild("LunarX") then pgui.LunarX:Destroy() end
local gui=Instance.new("ScreenGui",pgui)
gui.Name="LunarX"; gui.ResetOnSpawn=false; gui.IgnoreGuiInset=true
gui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling; gui.DisplayOrder=999

local W,H=580,440

local function cr(i,r)  local uc=Instance.new("UICorner",i); uc.CornerRadius=UDim.new(0,r or 6); return uc end
local function mkStroke(i,c,t) local s=Instance.new("UIStroke",i); s.Color=c or T.bord; s.Thickness=t or 1; return s end
local function pd(i,a,b,cc,d)
    local p=Instance.new("UIPadding",i)
    p.PaddingTop=UDim.new(0,a); p.PaddingBottom=UDim.new(0,b or a)
    p.PaddingLeft=UDim.new(0,cc or a); p.PaddingRight=UDim.new(0,d or cc or a)
end
local function vl(i,g) local l=Instance.new("UIListLayout",i); l.Padding=UDim.new(0,g or 4); l.SortOrder=Enum.SortOrder.LayoutOrder; return l end
local function hl(i,g)
    local l=Instance.new("UIListLayout",i); l.FillDirection=Enum.FillDirection.Horizontal
    l.Padding=UDim.new(0,g or 4); l.SortOrder=Enum.SortOrder.LayoutOrder; l.VerticalAlignment=Enum.VerticalAlignment.Center; return l
end
local function tw(inst,t2,props) TS:Create(inst,TweenInfo.new(t2,Enum.EasingStyle.Quart),props):Play() end

-- ── floating "return to position" button ─────────────────────────────────────
local retF=Instance.new("Frame",gui)
retF.Size=UDim2.new(0,170,0,34); retF.Position=UDim2.new(0.5,-84,1,-54)
retF.BackgroundColor3=T.acc; retF.BorderSizePixel=0; retF.Visible=false; retF.ZIndex=1000
cr(retF,8); reg(retF,"acc"); returnBtnRef=retF
local retB=Instance.new("TextButton",retF)
retB.Size=UDim2.new(1,0,1,0); retB.BackgroundTransparency=1
retB.Text="[ return to position ]"; retB.Font=Enum.Font.GothamBold; retB.TextSize=12
retB.TextColor3=T.atxt; retB.AutoButtonColor=false; retB.BorderSizePixel=0
reg(retB,"atxt","TextColor3")
retB.MouseButton1Click:Connect(function()
    local h=hrp(); if h and returnCF then h.CFrame=returnCF; returnCF=nil; retF.Visible=false end
end)

-- ── floating auto farm HUD (bottom centre, above any return button) ───────────
local farmHud=Instance.new("TextButton",gui)
farmHud.Size=UDim2.new(0,200,0,30); farmHud.Position=UDim2.new(0.5,-100,1,-96)
farmHud.BackgroundColor3=T.panel; farmHud.BorderSizePixel=0
farmHud.Font=Enum.Font.GothamBold; farmHud.TextSize=11
farmHud.Text="auto farm: off"; farmHud.TextColor3=T.sub
farmHud.AutoButtonColor=false; farmHud.ZIndex=999
cr(farmHud,8); reg(farmHud,"panel"); reg(farmHud,"sub","TextColor3")
local farmHudStroke=mkStroke(farmHud,T.bord); reg(farmHudStroke,"bord","Color")
farmHudLbl=farmHud  -- we reuse the button's Text as the label

farmHud.MouseButton1Click:Connect(function()
    cfg.AutoStarCrater=not cfg.AutoStarCrater
    farmHud.Text=cfg.AutoStarCrater and "auto farm: on  ●" or "auto farm: off"
    TS:Create(farmHud,.2,{TextColor3=cfg.AutoStarCrater and T.green or T.sub}):Play()
end)

-- keep hud text fresh
task.spawn(function()
    while _G.__lx and task.wait(1) do
        if not cfg.AutoStarCrater then
            farmHud.Text="auto farm: off"
            TS:Create(farmHud,.2,{TextColor3=T.sub}):Play()
        end
    end
end)

-- ── root window ───────────────────────────────────────────────────────────────
local root=Instance.new("Frame",gui)
root.Size=UDim2.new(0,W,0,H); root.Position=UDim2.new(0.5,-W/2,0.45,-H/2)
root.BackgroundColor3=T.bg; root.Active=true; root.Draggable=true
root.ClipsDescendants=true; root.BorderSizePixel=0
local rootStroke=mkStroke(root,T.bord); reg(rootStroke,"bord","Color"); reg(root,"bg")

-- gradient accent strip
local strip=Instance.new("Frame",root)
strip.Size=UDim2.new(1,0,0,2); strip.BorderSizePixel=0
local sg=Instance.new("UIGradient",strip)
sg.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,T.acc),ColorSequenceKeypoint.new(0.5,T.purp),ColorSequenceKeypoint.new(1,T.cyan)}
reg(strip,"acc")

local tbar=Instance.new("Frame",root)
tbar.Size=UDim2.new(1,0,0,42); tbar.Position=UDim2.new(0,0,0,2)
tbar.BackgroundColor3=T.panel; tbar.BorderSizePixel=0; reg(tbar,"panel")
local tbarDiv=Instance.new("Frame",tbar)
tbarDiv.Size=UDim2.new(1,0,0,1); tbarDiv.Position=UDim2.new(0,0,1,-1)
tbarDiv.BackgroundColor3=T.bord; tbarDiv.BorderSizePixel=0; reg(tbarDiv,"bord")

local tTitle=Instance.new("TextLabel",tbar)
tTitle.Size=UDim2.new(0,80,1,0); tTitle.Position=UDim2.new(0,14,0,0)
tTitle.BackgroundTransparency=1; tTitle.Text="lunar x"
tTitle.Font=Enum.Font.GothamBold; tTitle.TextSize=14; tTitle.TextColor3=T.txt
tTitle.TextXAlignment=Enum.TextXAlignment.Left; reg(tTitle,"txt","TextColor3")

local tSub=Instance.new("TextLabel",tbar)
tSub.Size=UDim2.new(0,160,1,0); tSub.Position=UDim2.new(0,96,0,0)
tSub.BackgroundTransparency=1; tSub.Text="fishing"
tSub.Font=Enum.Font.Gotham; tSub.TextSize=12; tSub.TextColor3=T.sub
tSub.TextXAlignment=Enum.TextXAlignment.Left; reg(tSub,"sub","TextColor3")

local function mkWinBtn(xoff,sym,hcol)
    local b=Instance.new("TextButton",tbar)
    b.Size=UDim2.new(0,28,0,28); b.Position=UDim2.new(1,xoff,0.5,-14)
    b.BackgroundColor3=T.el; b.Text=sym; b.TextSize=13; b.Font=Enum.Font.GothamBold
    b.TextColor3=T.sub; b.AutoButtonColor=false; b.BorderSizePixel=0; cr(b,5); reg(b,"el")
    b.MouseEnter:Connect(function() tw(b,.12,{BackgroundColor3=hcol,TextColor3=T.atxt}) end)
    b.MouseLeave:Connect(function() tw(b,.12,{BackgroundColor3=T.el,TextColor3=T.sub}) end)
    return b
end
local closeB=mkWinBtn(-36,"x",T.red)
local minB  =mkWinBtn(-70,"-",T.dim)
closeB.MouseButton1Click:Connect(function() saveCfg(); gui:Destroy() end)

local body=Instance.new("Frame",root)
body.Size=UDim2.new(1,0,1,-44); body.Position=UDim2.new(0,0,0,44)
body.BackgroundTransparency=1; body.ClipsDescendants=true

local minimised=false
local function setMinimised(v)
    minimised=v; body.Visible=not v
    tw(root,.2,{Size=v and UDim2.new(0,W,0,46) or UDim2.new(0,W,0,H)})
end
minB.MouseButton1Click:Connect(function() setMinimised(not minimised) end)

local side=Instance.new("Frame",body)
side.Size=UDim2.new(0,130,1,0); side.BackgroundColor3=T.panel; side.BorderSizePixel=0; reg(side,"panel")
local sideDiv=Instance.new("Frame",body)
sideDiv.Position=UDim2.new(0,130,0,0); sideDiv.Size=UDim2.new(0,1,1,0)
sideDiv.BackgroundColor3=T.bord; sideDiv.BorderSizePixel=0; reg(sideDiv,"bord")

local tabList=Instance.new("Frame",side)
tabList.Size=UDim2.new(1,0,1,-44); tabList.BackgroundTransparency=1
vl(tabList,1); pd(tabList,6,0,5,5)

-- theme dots
local themeArea=Instance.new("Frame",side)
themeArea.Size=UDim2.new(1,0,0,40); themeArea.Position=UDim2.new(0,0,1,-40)
themeArea.BackgroundColor3=T.panel; themeArea.BorderSizePixel=0; reg(themeArea,"panel")
local thDiv=Instance.new("Frame",themeArea)
thDiv.Size=UDim2.new(1,-16,0,1); thDiv.Position=UDim2.new(0,8,0,0)
thDiv.BackgroundColor3=T.bord; thDiv.BorderSizePixel=0; reg(thDiv,"bord")
local themeDots=Instance.new("Frame",themeArea)
themeDots.Size=UDim2.new(1,-12,0,24); themeDots.Position=UDim2.new(0,6,0,10)
themeDots.BackgroundTransparency=1
local tdl=hl(themeDots,5); tdl.HorizontalAlignment=Enum.HorizontalAlignment.Center
local THEME_ORDER={"Default","Midnight","Synthwave","Carbon","Ocean"}
local THEME_DOTCOL={Default=rgb(88,125,255),Midnight=rgb(145,90,255),Synthwave=rgb(255,45,200),Carbon=rgb(200,200,200),Ocean=rgb(30,195,225)}
for _,tn in THEME_ORDER do
    local d=Instance.new("TextButton",themeDots)
    d.Size=UDim2.new(0,14,0,14); d.BackgroundColor3=THEME_DOTCOL[tn]
    d.BorderSizePixel=0; d.Text=""; d.AutoButtonColor=false; cr(d,7)
    local ds=mkStroke(d,rgb(0,0,0),0)
    d.MouseEnter:Connect(function() tw(ds,.1,{Transparency=0.3,Thickness=2}) end)
    d.MouseLeave:Connect(function() tw(ds,.1,{Transparency=1}) end)
    d.MouseButton1Click:Connect(function()
        applyTheme(tn); cfg.Theme=tn
        local new=THEMES[tn]
        sg.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,new.acc),ColorSequenceKeypoint.new(0.5,new.purp),ColorSequenceKeypoint.new(1,new.cyan)}
    end)
end

local pHolder=Instance.new("Frame",body)
pHolder.Position=UDim2.new(0,131,0,0); pHolder.Size=UDim2.new(1,-131,1,0)
pHolder.BackgroundTransparency=1; pHolder.ClipsDescendants=true

-- ── tab system ────────────────────────────────────────────────────────────────
local tabs={}; local activeTabIdx=0
local TAB_LIST={
    {n="fishing",   idx=0},{n="auto farm", idx=1},{n="player",   idx=2},
    {n="teleport",  idx=3},{n="positions", idx=4},{n="events",   idx=5},
    {n="boat",      idx=6},{n="shop",      idx=7},{n="webhook",  idx=8},
    {n="settings",  idx=9},{n="options",   idx=10},{n="debug",   idx=11},
}

local function switchTab(idx)
    activeTabIdx=idx
    for _,t in tabs do
        local on=t.idx==idx
        t.indicator.BackgroundTransparency=on and 0 or 1
        tw(t.btn,.12,{TextColor3=on and T.acc or T.sub,BackgroundColor3=on and T.el or T.panel,BackgroundTransparency=on and 0 or 1})
        t.page.Visible=on
    end
    for _,t in tabs do if t.idx==idx then tSub.Text=t.n end end
end

local function newTab(def)
    local frame=Instance.new("Frame",tabList)
    frame.Size=UDim2.new(1,0,0,28); frame.BackgroundTransparency=1; frame.LayoutOrder=def.idx
    local ind=Instance.new("Frame",frame)
    ind.Size=UDim2.new(0,3,0,16); ind.Position=UDim2.new(0,0,0.5,-8)
    ind.BackgroundColor3=T.acc; ind.BorderSizePixel=0; ind.BackgroundTransparency=1; cr(ind,2); reg(ind,"acc")
    local btn=Instance.new("TextButton",frame)
    btn.Size=UDim2.new(1,-4,1,0); btn.Position=UDim2.new(0,4,0,0)
    btn.BackgroundColor3=T.panel; btn.BackgroundTransparency=1
    btn.Text=def.n; btn.Font=Enum.Font.GothamMedium; btn.TextSize=11
    btn.TextColor3=T.sub; btn.AutoButtonColor=false; btn.BorderSizePixel=0
    btn.TextXAlignment=Enum.TextXAlignment.Left; cr(btn,5); pd(btn,0,0,8); reg(btn,"panel")
    local page=Instance.new("ScrollingFrame",pHolder)
    page.Size=UDim2.new(1,0,1,0); page.BackgroundTransparency=1
    page.Visible=def.idx==0; page.ScrollBarThickness=2
    page.ScrollBarImageColor3=T.acc; page.AutomaticCanvasSize=Enum.AutomaticSize.Y
    page.CanvasSize=UDim2.new(0,0,0,0); page.BorderSizePixel=0
    page.ScrollingDirection=Enum.ScrollingDirection.Y
    vl(page,5); pd(page,10,10,12,12)
    btn.MouseEnter:Connect(function() if def.idx~=activeTabIdx then tw(btn,.08,{BackgroundTransparency=0,BackgroundColor3=T.el,TextColor3=T.txt}) end end)
    btn.MouseLeave:Connect(function() if def.idx~=activeTabIdx then tw(btn,.08,{BackgroundTransparency=1,TextColor3=T.sub}) end end)
    btn.MouseButton1Click:Connect(function() switchTab(def.idx) end)
    table.insert(tabs,{btn=btn,indicator=ind,page=page,idx=def.idx,n=def.n})
    return page
end

local pages={}
for _,def in TAB_LIST do pages[def.n]=newTab(def) end
switchTab(0)

-- ══════════════════════════════════════════════════════
--  COMPONENT BUILDERS
-- ══════════════════════════════════════════════════════
local function secLabel(parent,text)
    local f=Instance.new("Frame",parent)
    f.Size=UDim2.new(1,0,0,18); f.BackgroundTransparency=1
    local l=Instance.new("TextLabel",f)
    l.Size=UDim2.new(1,-2,1,0); l.BackgroundTransparency=1; l.Text=text:upper()
    l.Font=Enum.Font.GothamBold; l.TextSize=9; l.TextColor3=T.sub; l.TextXAlignment=Enum.TextXAlignment.Left
    reg(l,"sub","TextColor3")
    local line=Instance.new("Frame",f)
    line.Size=UDim2.new(1,0,0,1); line.Position=UDim2.new(0,0,1,-1)
    line.BackgroundColor3=T.bord; line.BorderSizePixel=0; reg(line,"bord")
end

local function mkRow(parent,h)
    local f=Instance.new("Frame",parent)
    f.Size=UDim2.new(1,0,0,h or 36); f.BackgroundColor3=T.el; f.BorderSizePixel=0; cr(f,7); reg(f,"el"); return f
end

-- toggle: knob and track use full theme colors
local function mkToggle(parent,label,sublabel,def,cb,ac)
    ac=ac or T.acc
    local row=mkRow(parent,sublabel and 44 or 36)
    local lf=Instance.new("Frame",row)
    lf.Size=UDim2.new(1,-62,1,0); lf.Position=UDim2.new(0,12,0,0); lf.BackgroundTransparency=1
    local ml=Instance.new("TextLabel",lf)
    ml.BackgroundTransparency=1; ml.Text=label
    ml.Font=Enum.Font.GothamMedium; ml.TextSize=12; ml.TextColor3=T.txt; ml.TextXAlignment=Enum.TextXAlignment.Left
    reg(ml,"txt","TextColor3")
    if sublabel then
        ml.Size=UDim2.new(1,0,0,18); ml.Position=UDim2.new(0,0,0,4)
        local sl=Instance.new("TextLabel",lf)
        sl.Size=UDim2.new(1,0,0,13); sl.Position=UDim2.new(0,0,0,21)
        sl.BackgroundTransparency=1; sl.Text=sublabel
        sl.Font=Enum.Font.Gotham; sl.TextSize=10; sl.TextColor3=T.sub; sl.TextXAlignment=Enum.TextXAlignment.Left
        reg(sl,"sub","TextColor3")
    else ml.Size=UDim2.new(1,0,1,0) end

    -- track uses T.dim when off (theme-aware)
    local track=Instance.new("Frame",row)
    track.Size=UDim2.new(0,40,0,22); track.Position=UDim2.new(1,-52,0.5,-11)
    track.BackgroundColor3=def and ac or T.dim; track.BorderSizePixel=0; cr(track,11)
    -- register off-state separately so theme updates it
    local function updateTrackReg(on)
        for i,r in colorRegs do
            if r.inst==track then table.remove(colorRegs,i); break end
        end
        table.insert(colorRegs,{inst=track,key=on and "acc" or "dim"})
    end
    updateTrackReg(def)

    -- knob uses T.atxt so it contrasts on any theme
    local knob=Instance.new("Frame",track)
    knob.Size=UDim2.new(0,16,0,16)
    knob.Position=def and UDim2.new(0,21,0,3) or UDim2.new(0,3,0,3)
    knob.BackgroundColor3=T.atxt; knob.BorderSizePixel=0; cr(knob,8)
    reg(knob,"atxt")

    local hit=Instance.new("TextButton",row)
    hit.Size=UDim2.new(1,0,1,0); hit.BackgroundTransparency=1; hit.Text=""; hit.AutoButtonColor=false; hit.BorderSizePixel=0
    row.MouseEnter:Connect(function() tw(row,.08,{BackgroundColor3=T.elh}) end)
    row.MouseLeave:Connect(function() tw(row,.08,{BackgroundColor3=T.el}) end)

    local state=def
    hit.MouseButton1Click:Connect(function()
        state=not state; cb(state)
        tw(track,.25,{BackgroundColor3=state and ac or T.dim})
        tw(knob, .25,{Position=state and UDim2.new(0,21,0,3) or UDim2.new(0,3,0,3)})
        updateTrackReg(state)
    end)
    return function(force)  -- refresh fn
        state=force
        tw(track,.25,{BackgroundColor3=state and ac or T.dim})
        tw(knob, .25,{Position=state and UDim2.new(0,21,0,3) or UDim2.new(0,3,0,3)})
        updateTrackReg(state)
    end
end

local function mkSlider(parent,label,mn,mx,def,cb,ac)
    ac=ac or T.acc
    local f=mkRow(parent,54)
    local hf=Instance.new("Frame",f); hf.Size=UDim2.new(1,-24,0,18); hf.Position=UDim2.new(0,12,0,7); hf.BackgroundTransparency=1
    local ll=Instance.new("TextLabel",hf); ll.Size=UDim2.new(1,-52,1,0); ll.BackgroundTransparency=1
    ll.Text=label; ll.Font=Enum.Font.GothamMedium; ll.TextSize=12; ll.TextColor3=T.txt; ll.TextXAlignment=Enum.TextXAlignment.Left
    reg(ll,"txt","TextColor3")
    local vl2=Instance.new("TextLabel",hf); vl2.Size=UDim2.new(0,46,1,0); vl2.Position=UDim2.new(1,-46,0,0)
    vl2.BackgroundTransparency=1; vl2.Text=tostring(def); vl2.Font=Enum.Font.GothamBold; vl2.TextSize=12
    vl2.TextColor3=ac; vl2.TextXAlignment=Enum.TextXAlignment.Right
    local trackBG=Instance.new("TextButton",f)
    trackBG.Size=UDim2.new(1,-24,0,4); trackBG.Position=UDim2.new(0,12,0,39)
    trackBG.BackgroundColor3=T.dim; trackBG.Text=""; trackBG.BorderSizePixel=0; cr(trackBG,2); reg(trackBG,"dim")
    local fill=Instance.new("Frame",trackBG)
    fill.Size=UDim2.new((def-mn)/(mx-mn),0,1,0); fill.BackgroundColor3=ac; fill.BorderSizePixel=0; cr(fill,2)
    local thumb=Instance.new("Frame",trackBG)
    thumb.Size=UDim2.new(0,11,0,11); thumb.AnchorPoint=Vector2.new(.5,.5)
    thumb.Position=UDim2.new((def-mn)/(mx-mn),0,.5,0)
    thumb.BackgroundColor3=T.atxt; thumb.BorderSizePixel=0; cr(thumb,6); reg(thumb,"atxt")
    local drag=false
    local function upd()
        local pct=math.clamp((UIS:GetMouseLocation().X-trackBG.AbsolutePosition.X)/trackBG.AbsoluteSize.X,0,1)
        fill.Size=UDim2.new(pct,0,1,0); thumb.Position=UDim2.new(pct,0,.5,0)
        local v=math.floor(mn+pct*(mx-mn)); vl2.Text=tostring(v); cb(v)
    end
    trackBG.InputBegan:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseButton1 then drag=true;upd() end end)
    UIS.InputEnded:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseButton1 then drag=false end end)
    UIS.InputChanged:Connect(function(i) if drag and i.UserInputType==Enum.UserInputType.MouseMovement then upd() end end)
    f.MouseEnter:Connect(function() tw(f,.08,{BackgroundColor3=T.elh}) end)
    f.MouseLeave:Connect(function() tw(f,.08,{BackgroundColor3=T.el}) end)
end

local function mkButton(parent,label,col,fn)
    local b=Instance.new("TextButton",parent)
    b.Size=UDim2.new(1,0,0,34); b.BackgroundColor3=col or T.el
    b.TextColor3=T.txt; b.Font=Enum.Font.GothamMedium; b.TextSize=12
    b.Text=label; b.AutoButtonColor=false; b.BorderSizePixel=0; cr(b,7)
    if not col then reg(b,"el") end; reg(b,"txt","TextColor3")
    b.MouseEnter:Connect(function() tw(b,.08,{BackgroundColor3=(col or T.el):Lerp(rgb(255,255,255),.08)}) end)
    b.MouseLeave:Connect(function() tw(b,.08,{BackgroundColor3=col or T.el}) end)
    b.MouseButton1Click:Connect(fn); return b
end

local function mkInput(parent,ph,def)
    local w=Instance.new("Frame",parent)
    w.Size=UDim2.new(1,0,0,34); w.BackgroundColor3=T.el; w.BorderSizePixel=0; cr(w,7); reg(w,"el")
    local brd=mkStroke(w,T.bord); reg(brd,"bord","Color")
    local tb=Instance.new("TextBox",w)
    tb.Size=UDim2.new(1,-14,1,0); tb.Position=UDim2.new(0,7,0,0)
    tb.BackgroundTransparency=1; tb.Text=def or ""; tb.PlaceholderText=ph
    tb.PlaceholderColor3=T.sub; tb.Font=Enum.Font.Gotham; tb.TextSize=12
    tb.TextColor3=T.txt; tb.ClearTextOnFocus=false; tb.BorderSizePixel=0
    reg(tb,"txt","TextColor3"); reg(tb,"sub","PlaceholderColor3")
    tb.Focused:Connect(function() tw(brd,.1,{Color=T.acc}) end)
    tb.FocusLost:Connect(function() tw(brd,.1,{Color=T.bord}) end)
    return w,tb
end

local function mkEntry(parent,label,isEv,fn)
    local b=Instance.new("TextButton",parent)
    b.Size=UDim2.new(1,0,0,30); b.BorderSizePixel=0; b.BackgroundColor3=T.el
    b.TextColor3=isEv and T.purp or T.txt; b.Font=Enum.Font.GothamMedium; b.TextSize=12
    b.Text="  "..(isEv and "[*] " or "> ")..label
    b.TextXAlignment=Enum.TextXAlignment.Left; b.AutoButtonColor=false; cr(b,7)
    if isEv then reg(b,"purp","TextColor3") else reg(b,"txt","TextColor3") end; reg(b,"el")
    b.MouseEnter:Connect(function() tw(b,.08,{BackgroundColor3=T.elh}) end)
    b.MouseLeave:Connect(function() tw(b,.08,{BackgroundColor3=T.el}) end)
    b.MouseButton1Click:Connect(fn); return b
end

local function mkCollapse(parent,title,isEv,openDef)
    local w=Instance.new("Frame",parent)
    w.Size=UDim2.new(1,0,0,0); w.BackgroundTransparency=1; w.AutomaticSize=Enum.AutomaticSize.Y
    local wl2=Instance.new("UIListLayout",w); wl2.SortOrder=Enum.SortOrder.LayoutOrder; wl2.Padding=UDim.new(0,2)
    local hdr=Instance.new("TextButton",w)
    hdr.Size=UDim2.new(1,0,0,26); hdr.LayoutOrder=0
    hdr.BackgroundColor3=T.elh; hdr.BorderSizePixel=0; hdr.AutoButtonColor=false; cr(hdr,7); reg(hdr,"elh")
    local hl2=Instance.new("TextLabel",hdr)
    hl2.Size=UDim2.new(1,-34,1,0); hl2.Position=UDim2.new(0,12,0,0)
    hl2.BackgroundTransparency=1; hl2.Font=Enum.Font.GothamBold; hl2.TextSize=9
    hl2.TextColor3=isEv and T.purp or T.sub; hl2.TextXAlignment=Enum.TextXAlignment.Left; hl2.Text=title:upper()
    reg(hl2,isEv and "purp" or "sub","TextColor3")
    local ar=Instance.new("TextLabel",hdr)
    ar.Size=UDim2.new(0,20,1,0); ar.Position=UDim2.new(1,-24,0,0)
    ar.BackgroundTransparency=1; ar.Font=Enum.Font.GothamBold; ar.TextSize=10; ar.TextColor3=T.sub
    ar.Text=openDef and "v" or ">"; reg(ar,"sub","TextColor3")
    local cnt=Instance.new("Frame",w)
    cnt.LayoutOrder=1; cnt.BackgroundTransparency=1
    cnt.Size=UDim2.new(1,0,0,0); cnt.AutomaticSize=Enum.AutomaticSize.Y; cnt.Visible=openDef; vl(cnt,2)
    local open=openDef
    hdr.MouseButton1Click:Connect(function() open=not open; cnt.Visible=open; ar.Text=open and "v" or ">" end)
    return cnt
end

local function mkInfoRow(parent,label,val)
    local row=mkRow(parent,32)
    local ll=Instance.new("TextLabel",row); ll.Size=UDim2.new(0.42,0,1,0); ll.Position=UDim2.new(0,12,0,0)
    ll.BackgroundTransparency=1; ll.Font=Enum.Font.GothamMedium; ll.TextSize=11
    ll.TextColor3=T.sub; ll.TextXAlignment=Enum.TextXAlignment.Left; ll.Text=label; reg(ll,"sub","TextColor3")
    local vl3=Instance.new("TextLabel",row); vl3.Size=UDim2.new(0.55,0,1,0); vl3.Position=UDim2.new(0.44,0,0,0)
    vl3.BackgroundTransparency=1; vl3.Font=Enum.Font.Gotham; vl3.TextSize=11
    vl3.TextColor3=T.txt; vl3.TextXAlignment=Enum.TextXAlignment.Right; vl3.Text=tostring(val)
    vl3.TextTruncate=Enum.TextTruncate.AtEnd; reg(vl3,"txt","TextColor3")
    return vl3
end

-- ══════════════════════════════════════════════════════
--  PAGES
-- ══════════════════════════════════════════════════════

-- ── fishing ───────────────────────────────────────────────────────────────────
local fp=pages["fishing"]
secLabel(fp,"automation")
mkToggle(fp,"Auto Cast",     "casts rod when idle",        cfg.AutoCast,    function(v) cfg.AutoCast=v     end,T.green)
mkToggle(fp,"Auto Shake",    "taps shake button",          cfg.AutoShake,   function(v) cfg.AutoShake=v    end,T.green)
mkToggle(fp,"Instant Bite",  "forces bite on bobber",      cfg.InstantBite, function(v) cfg.InstantBite=v  end,T.green)
mkToggle(fp,"Auto Win",      "locks bar to fish position", cfg.AutoWin,     function(v) cfg.AutoWin=v      end,T.green)
mkToggle(fp,"Perfect Catch", "forces perfect each reel",   cfg.PerfectCatch,function(v) cfg.PerfectCatch=v end,T.green)
secLabel(fp,"tuning")
mkSlider(fp,"Progress Speed",0,200,cfg.ProgressSpeed,function(v) cfg.ProgressSpeed=v end,T.green)

-- ── auto farm ─────────────────────────────────────────────────────────────────
local afp=pages["auto farm"]
secLabel(afp,"star crater farm")

local statusRow=mkRow(afp,40)
local statusDot=Instance.new("Frame",statusRow)
statusDot.Size=UDim2.new(0,7,0,7); statusDot.Position=UDim2.new(0,12,0.5,-3.5)
statusDot.BackgroundColor3=T.dim; statusDot.BorderSizePixel=0; cr(statusDot,4); reg(statusDot,"dim")
starStatusDot=statusDot
local statusLbl=Instance.new("TextLabel",statusRow)
statusLbl.Size=UDim2.new(1,-28,1,0); statusLbl.Position=UDim2.new(0,26,0,0)
statusLbl.BackgroundTransparency=1; statusLbl.Font=Enum.Font.Gotham; statusLbl.TextSize=11
statusLbl.TextColor3=T.sub; statusLbl.TextXAlignment=Enum.TextXAlignment.Left; statusLbl.Text="idle"
reg(statusLbl,"sub","TextColor3"); starStatusLbl=statusLbl

task.spawn(function()
    while _G.__lx and task.wait(1) do
        if cfg.AutoStarCrater then tw(statusDot,.3,{BackgroundColor3=T.green}); statusLbl.TextColor3=T.green
        else tw(statusDot,.3,{BackgroundColor3=T.dim}); statusLbl.TextColor3=T.sub end
    end
end)

mkToggle(afp,"Auto Farm Star Craters","tap the button below to toggle or press it in the hud", cfg.AutoStarCrater,function(v) cfg.AutoStarCrater=v end,T.yell)
mkToggle(afp,"Server Hop When Done",  "hop to a new server after all craters farmed",          cfg.StarCraterServerHop,function(v) cfg.StarCraterServerHop=v end,T.cyan)

secLabel(afp,"farm settings")
mkSlider(afp,"Farm Speed (lower = faster)",10,300,math.floor(cfg.FarmSpeed*100),function(v) cfg.FarmSpeed=v/100 end,T.yell)
mkSlider(afp,"Delay Between Craters (×0.1s)",0,30,math.floor(cfg.FarmDelay*10),function(v) cfg.FarmDelay=v/10 end,T.yell)
mkSlider(afp,"Start Delay (s)",0,30,cfg.StartDelay,function(v) cfg.StartDelay=v end,T.cyan)

secLabel(afp,"stats")
local srvRow=mkRow(afp,32)
local srvLbl2=Instance.new("TextLabel",srvRow)
srvLbl2.Size=UDim2.new(1,-16,1,0); srvLbl2.Position=UDim2.new(0,12,0,0)
srvLbl2.BackgroundTransparency=1; srvLbl2.Font=Enum.Font.Gotham; srvLbl2.TextSize=12; srvLbl2.TextColor3=T.txt
srvLbl2.TextXAlignment=Enum.TextXAlignment.Left; srvLbl2.Text="Servers farmed: "..starCraterServerCount
reg(srvLbl2,"txt","TextColor3")
task.spawn(function() while _G.__lx and task.wait(2) do if srvLbl2 then srvLbl2.Text="Servers farmed: "..starCraterServerCount end end end)

mkButton(afp,"[>] Scan for Craters Now",nil,function()
    local found=findStarCraters(); notify("auto farm",#found.." crater(s) found")
end)
mkButton(afp,"[X] Reset Farm Counter",nil,function()
    starCraterServerCount=0; saveFarmCount(); notify("farm","counter reset")
end)
mkButton(afp,"[>>] Force Server Hop",T.purp:Lerp(rgb(0,0,0),.5),function()
    notify("farm","hopping..."); doServerHop()
end)

-- ── player ────────────────────────────────────────────────────────────────────
local pp=pages["player"]
secLabel(pp,"movement")
mkSlider(pp,"Walk Speed",16,500,cfg.WalkSpeed,function(v) cfg.WalkSpeed=v end)
mkSlider(pp,"Jump Power",50,500,cfg.JumpPower,function(v) cfg.JumpPower=v end)
mkToggle(pp,"Walk on Water","stand on water surface",         cfg.WalkOnWater,function(v) cfg.WalkOnWater=v; if v then enableWater() else disableWater() end end)
mkToggle(pp,"No Clip",      "pass through walls",             cfg.NoClip,     function(v) cfg.NoClip=v; setNoClip(v) end)
mkToggle(pp,"Freeze",       "lock position — legs only",      cfg.Frozen,     function(v) cfg.Frozen=v; setFrozen(v) end)
secLabel(pp,"survival")
mkToggle(pp,"God Mode",  "block all damage",        cfg.GodMode, function(v) cfg.GodMode=v; setGodMode(v) end,T.yell)
mkToggle(pp,"Anti Void", "rescue if Y below -60",   cfg.AntiVoid,function(v) cfg.AntiVoid=v end,T.yell)
mkToggle(pp,"Anti AFK",  "alt key ping every 55s",  cfg.AntiAFK, function(v) cfg.AntiAFK=v end,T.yell)

-- ── teleport ──────────────────────────────────────────────────────────────────
local tpP=pages["teleport"]
secLabel(tpP,"zone teleport")
local _,searchTB=mkInput(tpP,"search zones...")
local zWrap=Instance.new("Frame",tpP); zWrap.BackgroundTransparency=1
zWrap.Size=UDim2.new(1,0,0,0); zWrap.AutomaticSize=Enum.AutomaticSize.Y; vl(zWrap,3)
local normalList=mkCollapse(zWrap,"zones",false,true)
local eventList =mkCollapse(zWrap,"event zones",true,true)
local function populateZones()
    for _,ch in normalList:GetChildren() do if ch:IsA("TextButton") then ch:Destroy() end end
    for _,ch in eventList:GetChildren()  do if ch:IsA("TextButton") then ch:Destroy() end end
    local q=searchTB.Text:lower(); local seen={}
    local function addLoc(name,cf2,isEv)
        if seen[name] or not cf2 then return end
        if q~="" and not name:lower():find(q,1,true) then return end
        seen[name]=true
        mkEntry(isEv and eventList or normalList,name,isEv,function()
            local h=hrp(); if h then h.CFrame=cf2+Vector3.new(0,8,0); notify("tp",name) end
        end)
    end
    local zones=workspace:FindFirstChild("zones")
    if zones then
        local pz=zones:FindFirstChild("player"); local fz2=zones:FindFirstChild("fishing")
        if pz then for _,z in pz:GetChildren() do addLoc(z.Name,getCF(z),false) end end
        if fz2 then for _,z in fz2:GetChildren() do addLoc(z.Name,getCF(z),true) end end
    end
    local world=workspace:FindFirstChild("world"); local evF=world and world:FindFirstChild("events")
    if evF then for _,e in evF:GetChildren() do addLoc(e.Name,getCF(e),true) end end
end
searchTB.FocusLost:Connect(populateZones)
mkButton(tpP,"[R] Refresh Zones",nil,populateZones); populateZones()
secLabel(tpP,"player teleport")
local plF=Instance.new("Frame",tpP); plF.BackgroundTransparency=1
plF.Size=UDim2.new(1,0,0,0); plF.AutomaticSize=Enum.AutomaticSize.Y; vl(plF,3)
local function refreshPlayers()
    for _,ch in plF:GetChildren() do if ch:IsA("TextButton") or ch:IsA("TextLabel") then ch:Destroy() end end
    for _,p in Players:GetPlayers() do
        if p==lp then continue end
        mkEntry(plF,p.Name,false,function()
            local tgt=p.Character and p.Character:FindFirstChild("HumanoidRootPart"); local h=hrp()
            if h and tgt then h.CFrame=tgt.CFrame+Vector3.new(0,3,0); notify("tp",p.Name) end
        end)
    end
end
mkButton(tpP,"[R] Refresh Players",nil,refreshPlayers); refreshPlayers()
secLabel(tpP,"coordinate teleport")
local cRow=Instance.new("Frame",tpP); cRow.Size=UDim2.new(1,0,0,34); cRow.BackgroundTransparency=1; hl(cRow,4)
local cInputs={}
for _,ax in {"X","Y","Z"} do
    local wrap=Instance.new("Frame",cRow); wrap.Size=UDim2.new(0.333,-3,1,0)
    wrap.BackgroundColor3=T.el; wrap.BorderSizePixel=0; cr(wrap,7); reg(wrap,"el")
    local tb=Instance.new("TextBox",wrap); tb.Size=UDim2.new(1,-8,1,0); tb.Position=UDim2.new(0,4,0,0)
    tb.BackgroundTransparency=1; tb.Text=""; tb.PlaceholderText=ax; tb.PlaceholderColor3=T.sub
    tb.Font=Enum.Font.Gotham; tb.TextSize=12; tb.TextColor3=T.txt; tb.ClearTextOnFocus=false; tb.BorderSizePixel=0
    reg(tb,"txt","TextColor3"); cInputs[ax]=tb
end
mkButton(tpP,"Teleport to Coords",T.acc,function()
    local x,y,z=tonumber(cInputs.X.Text),tonumber(cInputs.Y.Text),tonumber(cInputs.Z.Text)
    if not(x and y and z) then notify("tp","invalid coords"); return end
    local h=hrp(); if h then h.CFrame=CFrame.new(x,y,z) end
end)

-- ── positions ─────────────────────────────────────────────────────────────────
local posp=pages["positions"]
secLabel(posp,"save slots")
local slotWrap=Instance.new("Frame",posp); slotWrap.BackgroundTransparency=1
slotWrap.Size=UDim2.new(1,0,0,0); slotWrap.AutomaticSize=Enum.AutomaticSize.Y; vl(slotWrap,4)
local lastClickT={}; local retSlotRefresh=nil

local function buildSlot(i)
    local row=mkRow(slotWrap,36); row.LayoutOrder=i
    local nameLbl=Instance.new("TextLabel",row)
    nameLbl.Size=UDim2.new(1,-122,1,0); nameLbl.Position=UDim2.new(0,12,0,0)
    nameLbl.BackgroundTransparency=1; nameLbl.Font=Enum.Font.Gotham; nameLbl.TextSize=11
    nameLbl.TextXAlignment=Enum.TextXAlignment.Left; nameLbl.TextTruncate=Enum.TextTruncate.AtEnd
    local function refreshLbl()
        local name=slotNames[i] or ("slot "..i)
        if slots[i] then
            local p=slots[i].Position
            nameLbl.Text=name.."  "..math.floor(p.X).." "..math.floor(p.Y).." "..math.floor(p.Z)
            nameLbl.TextColor3=T.green
        else nameLbl.Text=name.."  (empty)"; nameLbl.TextColor3=T.dim end
    end
    refreshLbl()
    local function smBtn(xo,txt,col,fn2)
        local b=Instance.new("TextButton",row)
        b.Size=UDim2.new(0,34,0,24); b.Position=UDim2.new(1,xo,0.5,-12)
        b.BackgroundColor3=col; b.TextColor3=T.atxt; b.Font=Enum.Font.GothamBold
        b.TextSize=10; b.Text=txt; b.AutoButtonColor=false; b.BorderSizePixel=0; cr(b,6)
        reg(b,"atxt","TextColor3"); b.MouseButton1Click:Connect(fn2); return b
    end
    smBtn(-114,"[S]",T.green,function()
        local h=hrp(); if not h then return end
        slots[i]=h.CFrame; refreshLbl(); saveSlots()
        notify("pos","slot "..i.." saved")
        if retSlotRefresh then retSlotRefresh() end
    end)
    smBtn(-74,"[L]",T.cyan:Lerp(rgb(0,0,0),.45),function()
        if slots[i] then local h=hrp(); if h then h.CFrame=slots[i]; notify("pos","slot "..i) end
        else notify("pos","empty") end
    end)
    row.MouseEnter:Connect(function() tw(row,.08,{BackgroundColor3=T.elh}) end)
    row.MouseLeave:Connect(function() tw(row,.08,{BackgroundColor3=T.el}) end)
end
for i=1,slotCount do buildSlot(i) end
mkButton(posp,"[+] Add Slot",nil,function() slotCount=slotCount+1; buildSlot(slotCount); saveSlots() end)

-- ── events ────────────────────────────────────────────────────────────────────
local evp=pages["events"]
secLabel(evp,"auto teleport")
mkToggle(evp,"Auto Event TP","tps + freezes when event is detected",cfg.AutoEvent,function(v) cfg.AutoEvent=v end,T.purp)
mkButton(evp,"[X] Cancel / Skip Current Event",T.red:Lerp(rgb(0,0,0),.55),function()
    if activeEvent then
        cancelCurrentEvent()
        notify("events","skipped "..cancelledEvent)
    else notify("events","no active event") end
end)
secLabel(evp,"return slot after event")
-- return slot dropdown (rebuilt after slot saves)
local retWrap=Instance.new("Frame",evp); retWrap.Size=UDim2.new(1,0,0,0)
retWrap.BackgroundTransparency=1; retWrap.AutomaticSize=Enum.AutomaticSize.Y; vl(retWrap,2)
local retOpen=false
local retHdr=Instance.new("TextButton",retWrap)
retHdr.Size=UDim2.new(1,0,0,32); retHdr.BackgroundColor3=T.elh; retHdr.BorderSizePixel=0
retHdr.AutoButtonColor=false; cr(retHdr,7); reg(retHdr,"elh")
local retHdrLbl=Instance.new("TextLabel",retHdr)
retHdrLbl.Size=UDim2.new(1,-30,1,0); retHdrLbl.Position=UDim2.new(0,12,0,0)
retHdrLbl.BackgroundTransparency=1; retHdrLbl.Font=Enum.Font.GothamMedium; retHdrLbl.TextSize=12
retHdrLbl.TextColor3=T.txt; retHdrLbl.TextXAlignment=Enum.TextXAlignment.Left; retHdrLbl.Text="none"; reg(retHdrLbl,"txt","TextColor3")
local retHdrArr=Instance.new("TextLabel",retHdr)
retHdrArr.Size=UDim2.new(0,20,1,0); retHdrArr.Position=UDim2.new(1,-24,0,0)
retHdrArr.BackgroundTransparency=1; retHdrArr.Font=Enum.Font.GothamBold; retHdrArr.TextSize=10; retHdrArr.TextColor3=T.sub; retHdrArr.Text="v"; reg(retHdrArr,"sub","TextColor3")
local retCnt=Instance.new("Frame",retWrap); retCnt.BackgroundColor3=T.panel; retCnt.BorderSizePixel=0
retCnt.Size=UDim2.new(1,0,0,0); retCnt.AutomaticSize=Enum.AutomaticSize.Y; retCnt.Visible=false
cr(retCnt,7); reg(retCnt,"panel"); vl(retCnt,2); pd(retCnt,4,4,4,4)
retHdr.MouseButton1Click:Connect(function() retOpen=not retOpen; retCnt.Visible=retOpen; retHdrArr.Text=retOpen and "v" or ">" end)
local function rebuildRetSlots()
    for _,ch in retCnt:GetChildren() do if ch:IsA("TextButton") then ch:Destroy() end end
    local function rBtn(txt,fn)
        local b=Instance.new("TextButton",retCnt)
        b.Size=UDim2.new(1,0,0,28); b.BackgroundColor3=T.el; b.BorderSizePixel=0
        b.AutoButtonColor=false; b.TextColor3=T.txt; b.Font=Enum.Font.GothamMedium
        b.TextSize=12; b.Text=txt; b.TextXAlignment=Enum.TextXAlignment.Left; cr(b,6)
        reg(b,"el"); reg(b,"txt","TextColor3")
        b.MouseEnter:Connect(function() tw(b,.08,{BackgroundColor3=T.elh}) end)
        b.MouseLeave:Connect(function() tw(b,.08,{BackgroundColor3=T.el}) end)
        b.MouseButton1Click:Connect(fn)
    end
    rBtn("  none",function() cfg.EventReturnSlot=nil; retHdrLbl.Text="none"; retOpen=false; retCnt.Visible=false; retHdrArr.Text=">" end)
    for i=1,slotCount do
        local name=slotNames[i] or ("slot "..i)
        local disp="  ["..i.."]  "..name..(slots[i] and "" or "  (empty)")
        rBtn(disp,function() cfg.EventReturnSlot=i; retHdrLbl.Text="["..i.."] "..name; retOpen=false; retCnt.Visible=false; retHdrArr.Text=">" end)
    end
end
retSlotRefresh=rebuildRetSlots; rebuildRetSlots()

secLabel(evp,"event targets")
for _,ev in EVENTS do
    mkToggle(evp,ev.name,nil,eventEnabled[ev.name],function(v) eventEnabled[ev.name]=v end,T.purp)
end

-- ── boat ──────────────────────────────────────────────────────────────────────
local bp=pages["boat"]
secLabel(bp,"flight")
mkToggle(bp,"Boat Fly","WASD = move  E/Q = up/down",cfg.BoatFly,    function(v) cfg.BoatFly=v; setBoatFly(v) end,T.cyan)
mkSlider(bp,"Fly Speed",10,600,cfg.BoatFlySpeed,                    function(v) cfg.BoatFlySpeed=v end,T.cyan)
secLabel(bp,"utilities")
mkToggle(bp,"Boat No Clip","disable boat collision",  cfg.BoatNoClip,function(v) cfg.BoatNoClip=v; setBoatNoClip(v) end)
mkToggle(bp,"Freeze Boat", "lock boat CFrame",        cfg.BoatFreeze,function(v) cfg.BoatFreeze=v; setBoatFreeze(v) end)

-- ── shop ──────────────────────────────────────────────────────────────────────
local shp=pages["shop"]
secLabel(shp,"items — tps + presses E, then [return] appears")
local crateList=mkCollapse(shp,"bait crates",false,true)
for _,n in {"Bait Crate","Tropical Bait Crate","Coral Geode"} do
    mkEntry(crateList,n,false,function() task.spawn(useInteractable,n) end)
end
local totemList=mkCollapse(shp,"totems",false,true)
for _,n in {"Sundial Totem","Clearcast Totem","Aurora Totem","Blizzard Totem","Meteor Totem","Starfall Totem"} do
    mkEntry(totemList,n,false,function() task.spawn(useInteractable,n) end)
end
secLabel(shp,"scan workspace")
local scanList=mkCollapse(shp,"scan results",false,false)
mkButton(shp,"[S] Scan world.interactables",nil,function()
    for _,ch in scanList:GetChildren() do if ch:IsA("TextButton") then ch:Destroy() end end
    local names={}; local world=workspace:FindFirstChild("world"); local ints=world and world:FindFirstChild("interactables")
    if ints then for _,v in ints:GetChildren() do if v:IsA("Model") or v:IsA("BasePart") or v:IsA("Folder") then table.insert(names,v.Name) end end end
    table.sort(names)
    for _,name in names do mkEntry(scanList,name,false,function() task.spawn(useInteractable,name) end) end
    scanList.Visible=true; notify("shop",#names.." items")
end)

-- ── webhook ───────────────────────────────────────────────────────────────────
local wh=pages["webhook"]
secLabel(wh,"discord webhook")
local _,urlTB=mkInput(wh,"paste discord webhook url...",cfg.WebhookURL)
urlTB.FocusLost:Connect(function() cfg.WebhookURL=urlTB.Text end)
mkButton(wh,"[>] Send Test Ping",T.acc:Lerp(rgb(0,0,0),.5),function() sendWebhook("test"); notify("webhook","sent") end)
mkToggle(wh,"Enable Webhook",     nil,cfg.WHEnabled,   function(v) cfg.WHEnabled=v end)
mkToggle(wh,"Level Up",           nil,cfg.WHShowLevel, function(v) cfg.WHShowLevel=v end)
mkToggle(wh,"Show Catch Count",   nil,cfg.WHShowCatch, function(v) cfg.WHShowCatch=v end)
mkToggle(wh,"Show Time to Level", nil,cfg.WHShowTime,  function(v) cfg.WHShowTime=v  end)
mkToggle(wh,"Ping on Event Found",nil,cfg.WHEventStart,function(v) cfg.WHEventStart=v end)
mkToggle(wh,"Auto Farm Complete", nil,cfg.WHAutoFarm,  function(v) cfg.WHAutoFarm=v  end)

-- ── settings ──────────────────────────────────────────────────────────────────
local setp=pages["settings"]
secLabel(setp,"presets")
mkButton(setp,"[S] Save Preset",T.green:Lerp(rgb(0,0,0),.55),function()
    saveCfg(); saveSlots(); notify("settings","saved to lunarx/cfg.json")
end)
mkButton(setp,"[L] Load Preset",T.acc:Lerp(rgb(0,0,0),.55),function()
    loadCfg(); loadSlots(); notify("settings","loaded")
end)
mkButton(setp,"[X] Export to Clipboard",nil,function()
    local t={}; for k,v in cfg do if type(v)~="nil" then t[k]=v end end
    pcall(setclipboard, HTTP:JSONEncode(t)); notify("settings","copied")
end)
mkToggle(setp,"Auto Load on Start","loads lunarx/cfg.json when script runs",cfg.AutoLoadPreset,function(v) cfg.AutoLoadPreset=v; saveCfg() end)
secLabel(setp,"themes")
for tn,_ in THEMES do
    mkButton(setp,"Theme: "..tn,nil,function()
        applyTheme(tn); cfg.Theme=tn
        local new=THEMES[tn]
        sg.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,new.acc),ColorSequenceKeypoint.new(0.5,new.purp),ColorSequenceKeypoint.new(1,new.cyan)}
    end)
end

-- ── options ───────────────────────────────────────────────────────────────────
local op=pages["options"]
secLabel(op,"profile")
local profileCard=mkRow(op,64)
local avatar=Instance.new("ImageLabel",profileCard)
avatar.Size=UDim2.new(0,50,0,50); avatar.Position=UDim2.new(0,8,0.5,-25)
avatar.BackgroundColor3=T.elh; avatar.BorderSizePixel=0; cr(avatar,6); reg(avatar,"elh")
avatar.Image="rbxthumb://type=AvatarHeadShot&id="..lp.UserId.."&w=420&h=420"; avatar.ScaleType=Enum.ScaleType.Fit
local nameStk=Instance.new("Frame",profileCard)
nameStk.Size=UDim2.new(1,-68,1,0); nameStk.Position=UDim2.new(0,64,0,0); nameStk.BackgroundTransparency=1; vl(nameStk,2); pd(nameStk,10,0,0)
local dispLbl=Instance.new("TextLabel",nameStk); dispLbl.Size=UDim2.new(1,0,0,20)
dispLbl.BackgroundTransparency=1; dispLbl.Font=Enum.Font.GothamBold; dispLbl.TextSize=14
dispLbl.TextColor3=T.txt; dispLbl.TextXAlignment=Enum.TextXAlignment.Left; dispLbl.Text=lp.DisplayName; reg(dispLbl,"txt","TextColor3")
local userLbl=Instance.new("TextLabel",nameStk); userLbl.Size=UDim2.new(1,0,0,16)
userLbl.BackgroundTransparency=1; userLbl.Font=Enum.Font.Gotham; userLbl.TextSize=12
userLbl.TextColor3=T.sub; userLbl.TextXAlignment=Enum.TextXAlignment.Left; userLbl.Text="@"..lp.Name.."  ·  ID: "..lp.UserId; reg(userLbl,"sub","TextColor3")
secLabel(op,"server")
mkInfoRow(op,"Place ID",game.PlaceId)
mkInfoRow(op,"Job ID",game.JobId:sub(1,18).."...")
local pingRow=mkInfoRow(op,"Ping","--")
task.spawn(function()
    while _G.__lx and task.wait(2) do
        if pingRow and pingRow.Parent then
            local diff=math.abs((tick()-workspace:GetServerTimeNow())*1000)
            pingRow.Text=math.floor(diff).."ms"
        end
    end
end)
secLabel(op,"navigation")
mkButton(op,"[R] Rejoin",T.yell:Lerp(rgb(0,0,0),.55),function()
    local qot=queue_on_teleport or queueonteleport or (syn and syn.queue_on_teleport) or (fluxus and fluxus.queue_on_teleport)
    if qot then pcall(qot,'loadstring(game:HttpGet("https://raw.githubusercontent.com/nick2132131/rscripts/refs/heads/main/fisch"))()') end
    task.wait(0.5); TeleServ:Teleport(game.PlaceId,lp)
end)
mkButton(op,"[>>] Server Hop",T.purp:Lerp(rgb(0,0,0),.5),function() notify("hop","hopping..."); doServerHop() end)

-- ── debug ─────────────────────────────────────────────────────────────────────
local dbgP=pages["debug"]
secLabel(dbgP,"live log  —  all in lunarx/ folder")
local dbgFrame=mkRow(dbgP,320); dbgFrame.Size=UDim2.new(1,0,0,320); dbgFrame.BackgroundColor3=T.panel; reg(dbgFrame,"panel")
debugLbl=Instance.new("TextLabel",dbgFrame)
debugLbl.Size=UDim2.new(1,-10,1,-8); debugLbl.Position=UDim2.new(0,5,0,4)
debugLbl.BackgroundTransparency=1; debugLbl.Font=Enum.Font.Code; debugLbl.TextSize=10
debugLbl.TextColor3=T.green; debugLbl.TextXAlignment=Enum.TextXAlignment.Left
debugLbl.TextYAlignment=Enum.TextYAlignment.Top; debugLbl.TextWrapped=true; debugLbl.Text="[log ready]"
reg(debugLbl,"green","TextColor3")
mkButton(dbgP,"[C] Clear Log",nil,function() debugLog={}; debugLbl.Text="[cleared]" end)

-- ══════════════════════════════════════════════════════
--  KEYBIND + RUNTIME LOOPS
-- ══════════════════════════════════════════════════════
UIS.InputBegan:Connect(function(i,gpe)
    if gpe then return end
    if i.KeyCode==Enum.KeyCode.LeftControl then root.Visible=not root.Visible end
end)

-- auto shake
task.spawn(function()
    while _G.__lx and task.wait(0.1) do
        if not cfg.AutoShake then continue end
        pcall(function()
            local sg2=pgui:FindFirstChild("shakeui"); if not sg2 then return end
            local function findBtn(p)
                for _,c in p:GetChildren() do
                    if (c:IsA("TextButton") or c:IsA("ImageButton")) and c.Visible and c.AbsoluteSize.X>10 then return c end
                    local r=findBtn(c); if r then return r end
                end
            end
            local b=findBtn(sg2)
            if b then
                local cx=b.AbsolutePosition.X+b.AbsoluteSize.X/2; local cy=b.AbsolutePosition.Y+b.AbsoluteSize.Y/2
                VIM:SendMouseButtonEvent(cx,cy,0,true,game,1); task.wait(0.01)
                VIM:SendMouseButtonEvent(cx,cy,0,false,game,1)
            end
        end)
    end
end)

-- instant bite
task.spawn(function()
    while _G.__lx and task.wait(0.05) do
        if cfg.InstantBite then
            pcall(function()
                for _,v in workspace:GetDescendants() do
                    if v.Name=="Bobber" and v:GetAttribute("Owner")==lp.UserId then
                        if v:GetAttribute("CanBite")==true then v:SetAttribute("BiteTime",1) end
                    end
                end
            end)
        end
    end
end)

-- auto win
RunService.RenderStepped:Connect(function()
    if cfg.AutoWin and RC and RC.ActiveReel then
        pcall(function()
            local r=RC.ActiveReel; r.barPosition=r.fishPosition
            r.progressefficiency=cfg.ProgressSpeed; r.perfect=cfg.PerfectCatch
        end)
    end
end)

-- auto cast
task.spawn(function()
    while _G.__lx and task.wait(0.1) do
        if cfg.AutoCast and castRemote and RC and not RC.ActiveReel and not pgui:FindFirstChild("shakeui") then
            if lp.Character and lp.Character:FindFirstChildWhichIsA("Tool") then
                pcall(function() castRemote:InvokeServer(100,cfg.PerfectCatch) end)
            end
        end
    end
end)

-- speed / jump
RunService.Heartbeat:Connect(function()
    local h,r=hum(),hrp()
    if h and r and not cfg.Frozen then h.WalkSpeed=cfg.WalkSpeed; h.JumpPower=cfg.JumpPower; h.UseJumpPower=true end
end)

-- respawn
lp.CharacterAdded:Connect(function()
    task.wait(1); origCollide={}
    if cfg.WalkOnWater then waterPart=nil; enableWater() end
    if cfg.NoClip      then setNoClip(true)    end
    if cfg.GodMode     then setGodMode(true)   end
    if cfg.Frozen      then setFrozen(true)    end
    if cfg.BoatFly     then setBoatFly(true)   end
    if cfg.BoatNoClip  then setBoatNoClip(true)end
    dbg("respawned")
end)

-- apply saved theme
if cfg.Theme and THEMES[cfg.Theme] then
    applyTheme(cfg.Theme)
    local th=THEMES[cfg.Theme]
    sg.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,th.acc),ColorSequenceKeypoint.new(0.5,th.purp),ColorSequenceKeypoint.new(1,th.cyan)}
end

local function prdbg(text)
   if not text ~= nil then
      print(text)
      dbg(text)
    else 
        return 
    end
end

-- restore systems from saved cfg
if cfg.GodMode     then setGodMode(true)    end
if cfg.NoClip      then setNoClip(true)     end
if cfg.Frozen      then setFrozen(true)     end
if cfg.WalkOnWater then enableWater()       end
if cfg.BoatFly     then setBoatFly(true)    end
if cfg.BoatNoClip  then setBoatNoClip(true) end
if cfg.AutoStarCrater then setMinimised(true) end

prdbg("lunar x v5 loaded")
